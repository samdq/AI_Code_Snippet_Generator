from setuptools import setup, find_packages

setup(
    name='AI_Code_Snippet_Generator',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        'openai',
        # Other dependencies
    ],
)
